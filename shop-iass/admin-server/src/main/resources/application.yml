server:
  port: 10086
spring:
  application:
    name: admin-server
eureka:
  client:
    service-url:
     defaultZone: http://admin:admin@localhost:8761/eureka
management:
  endpoints:
    web:
      exposure:
        include: '*'